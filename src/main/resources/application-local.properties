#spring.datasource.url=jdbc:mysql://localhost:3306/housie
#spring.datasource.username=root
#spring.datasource.password=password

## PostgreSQL (local)
# You can override these with env vars: LOCAL_DB_URL, LOCAL_DB_USER, LOCAL_DB_PASSWORD
spring.datasource.url=${LOCAL_DB_URL:jdbc:postgresql://localhost:5432/housie?sslmode=disable}
spring.datasource.username=${LOCAL_DB_USER:paritosh}
spring.datasource.password=${LOCAL_DB_PASSWORD:password}
spring.datasource.platform=postgres

custom.scheme=http
custom.host=localhost:8080
